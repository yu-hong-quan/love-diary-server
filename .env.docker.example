# 服务器部署：复制为 .env，与 docker-compose.yml 放在同一目录
# cp .env.docker.example .env
#
# 镜像由 GitHub Actions 推送到 ghcr.io/yu-hong-quan/love-diary-go:latest
# 服务器执行：docker compose pull && docker compose up -d

PORT=3000
GIN_MODE=release

# 云数据库地址（勿把真实密码提交到 Git，只写在服务器上的 .env 里）
DB_HOST=154.48.226.121
DB_PORT=5432
DB_USER=love-diary-sql
DB_PASSWORD=G3pfnFB8C8Nn
DB_NAME=love-diary-sql

JWT_SECRET=请改为随机长字符串
