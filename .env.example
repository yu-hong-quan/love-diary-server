# 复制本文件为 .env 后填写（不要提交 .env 到 Git）
#   copy .env.example .env

PORT=3000
GIN_MODE=debug

# 图片落盘目录（Docker 建议挂载卷到 /app/uploads）
UPLOAD_DIR=./uploads

# 远程 PostgreSQL（不要用 localhost，除非本机真的装了 PG）
DB_HOST=154.48.226.121
DB_PORT=5432
DB_USER=love-diary-sql
DB_PASSWORD=G3pfnFB8C8Nn
DB_NAME=love-diary-sql

JWT_SECRET=love-diary-secret-key
